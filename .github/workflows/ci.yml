name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install root dependencies
        run: npm ci

      - name: Install worker dependencies
        run: npm ci
        working-directory: worker

      - name: Lint
        run: npm run lint

      - name: Build dist (generates worker/src/template.ts)
        run: npm run build

      - name: Typecheck worker
        run: npm run typecheck
        working-directory: worker

      - name: Parser tests
        run: npm test

      - name: Worker tests
        run: npm test
        working-directory: worker
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
