{
  "name": "glasspane",
  "version": "0.1.0",
  "description": "Browser teleprompter with Cloudflare Workers backend",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "node scripts/build.js",
    "lint": "biome check src tests scripts worker/src worker/tests"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.2",
    "dompurify": "^3.0.0",
    "vitest": "^2.0.0"
  }
}
